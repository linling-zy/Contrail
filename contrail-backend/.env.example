# ============================================
# 环境变量配置示例文件
# ============================================
# 复制此文件为 .env 并填入实际值
# 注意：.env 文件已加入 .gitignore，不会被提交到版本控制

# ============================================
# Flask 应用配置
# ============================================
FLASK_ENV=development
# 可选值: development, production

# ============================================
# 应用密钥（生产环境必须设置）
# ============================================
SECRET_KEY=your-secret-key-here-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-here-change-in-production

# ============================================
# RSA 加密密钥（生产环境必须设置）
# ============================================
# RSA私钥（PEM格式），用于解密前端加密的密码
# 生成方式：
#   1. 使用 openssl: openssl genrsa -out private_key.pem 2048
#   2. 将私钥内容（包括 -----BEGIN PRIVATE KEY----- 和 -----END PRIVATE KEY-----）
#      复制到此处，并将所有换行符替换为 \n
# 示例格式：
# RSA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----"
#
# 注意：
#   - 生产环境必须设置此变量，不允许自动生成
#   - 开发环境如果不设置，会自动生成密钥文件（不推荐）
#   - 私钥必须保密，不要提交到版本控制系统
RSA_PRIVATE_KEY=

# ============================================
# 数据库配置
# ============================================
# 开发环境（SQLite）
DEV_DATABASE_URL=sqlite:///contrail_dev.db

# 生产环境（MySQL）
# DATABASE_URL=mysql+pymysql://username:password@host:port/database

# ============================================
# MinIO/S3 对象存储配置
# ============================================
MINIO_ENDPOINT=http://127.0.0.1:9000
MINIO_BUCKET=student-certificates
MINIO_ACCESS_KEY=your-minio-access-key
MINIO_SECRET_KEY=your-minio-secret-key
MINIO_REGION=us-east-1
MINIO_PRESIGN_EXPIRES=3600
# MINIO_SECURE=true  # 是否使用 HTTPS（可选，默认根据 ENDPOINT 自动推断）
