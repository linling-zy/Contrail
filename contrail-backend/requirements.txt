# Flask 核心框架
Flask==2.3.3  # 使用 2.3.x 以兼容 Flask-APScheduler
Werkzeug==2.3.7  # 包含密码哈希功能，与 Flask 2.3.3 兼容

# 数据库 ORM
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.23
Flask-Migrate==4.0.5  # 数据库迁移工具

# 定时任务
Flask-APScheduler==1.13.0
APScheduler==3.10.4

# JWT 鉴权
Flask-JWT-Extended==4.6.0

# 序列化 (可选)
Flask-Marshmallow==0.15.0
marshmallow==3.20.1
marshmallow-sqlalchemy==0.29.0

# MySQL 驱动 (生产环境)
PyMySQL==1.1.0

# 环境变量管理
python-dotenv==1.0.0

# MinIO (Presigned URL)
minio==7.2.10

# RSA 加密
cryptography==42.0.0
